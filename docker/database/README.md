To configure the database, default environment variables
must be set in a file called ".env" under the database directory.

```
POSTGRES_PASSWORD=
POSTGRES_USER=
POSTGRES_DB=
PGDATA=/var/lib/postgresql/data/pgdata
```
